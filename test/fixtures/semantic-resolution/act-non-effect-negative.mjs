export const executor = workflow.executor;
export const commit = metadata.commit;
export const send = labels.send;
export const mutation = record.mutation;

export function execute(task) {
  return task;
}
