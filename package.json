{
  "name": "@avgl/core",
  "version": "0.4.0",
  "description": "Evidence-bound repository analysis and human-first projections for AVGL",
  "type": "module",
  "bin": {
    "avgl": "./bin/avgl.js"
  },
  "exports": "./src/index.js",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/*.js && node --check bin/avgl.js && node --check api/*.mjs && node --check api/github-app/*.mjs && node --check web/*.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
